
FROM docker.litellm.ai/berriai/litellm:main-latest

COPY infra/multi-ai/litellm-config.yaml /app/config.yaml

EXPOSE 4000

ENTRYPOINT ["/bin/sh", "-c"]
CMD ["exec litellm --config /app/config.yaml --host 0.0.0.0 --port ${PORT:-4000}"]
